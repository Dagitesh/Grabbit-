import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  /// Primary brand color
  static const Color primary = Color(0xFF2D5927);

  /// Scaffold / body background
  static const Color scaffoldBackground = Color(0xFFF6F7F6);

  /// Cards and foreground surfaces
  static const Color cardBackground = Color(0xFFFFFFFF);

  /// Timer and Reserve button (orange)
  static const Color timerOrange = Color(0xFFF97316);
}
